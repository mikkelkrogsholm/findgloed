# Glød Issue-Loop — Progress Tracker

**Startet:** 5. maj 2026
**Orkestrator:** Claude (auto mode + ScheduleWakeup safety net)
**Base branch:** `feature/platform-fase-1-4`

## Workflow pr. pakke

1. Opret epic-branch fra `feature/platform-fase-1-4`: `epic/pakke-N-<slug>`
2. Send subagent ud med konkret brief
3. Når agent er færdig: tjek tests, type-check, visuel verifikation hvor relevant
4. Hvis grønt: merge tilbage til `feature/platform-fase-1-4`
5. Opdatér denne fil
6. Næste pakke

## Status pr. pakke

| # | Pakke | Branch | Status | Agent-ID | Noter |
|---|-------|--------|--------|----------|-------|
| 1 | Make verification real | `epic/pakke-1-verification` | 🟢 MERGED | done | A6, A7, A8, A21, A23 — 45/45 tests grønne, merged til feature-branch |
| 2 | Couple as first-class | `epic/pakke-2-couple` | 🟢 MERGED | done | A3, A4, C6, C24 — invitation-flow + par-profil-UI + plans-grupperet, 54/54 tests |
| 3 | Lag 2 (match) skal virke | `epic/pakke-3-match-layer` | 🟢 MERGED | done | A2, B1, B2, B48 — match-relation, incoming-interests, album-grants, 64/64 tests |
| 4 | Brand-rens (beslutning 1+9) | `epic/pakke-4-brand-rens` | 🟢 MERGED | done | A15, A16, B12, C33-C43 — 15 filer, ~30 erstatninger, 45/45 tests grønne |
| 5 | GDPR + dataansvar | `epic/pakke-5-gdpr` | 🟢 MERGED | done | A9, A10, A17, A19, C9 — 55/55 tests grønne, merged |
| 6 | Code of conduct + terms | `epic/pakke-6-coc-terms` | 🟢 MERGED | done | A5, B46 — CoC m. tabs, terms-side, footer, signup/landing/event-detail links, 61/61 tests grønne |
| 7 | Auth & rate-limiting | `epic/pakke-7-auth-hardening` | 🟢 MERGED | done | A14, A18, B13, B14, B20-B22 — cookies, webhook-stub, rate-limits, magic-bytes, HMAC |
| 8 | Skeleton + a11y | `epic/pakke-8-a11y-skeleton` | 🟢 DONE | done | A20, A22, B23, B25, B27, B29, B30, B31, C47, C48, C58 + native confirm/prompt → Dialog. 74/74 tests grønne (10 nye) |
| 9 | Admin UX | `epic/pakke-9-admin-ux` | ⚪ TODO | — | B8, B9, B10, B17, C30-C32 |
| 10 | Stripe live-readiness | `epic/pakke-10-stripe` | ⚪ TODO | — | A18, B37, C25-C29 |
| 11 | DB- & query-optimering | `epic/pakke-11-perf` | ⚪ TODO | — | A11, A12, A13, A24, B15, B16 |

Legend: ⚪ TODO · 🟡 PENDING (briefed) · 🟠 IN PROGRESS · 🟢 MERGED · 🔴 BLOCKED

## Beslutningsregler

- Hvis agent rapporterer fejl: send ny agent med præcis hvad der mangler
- Hvis tests fejler: send ny agent med "fix testene"
- Hvis blokeret af brugerinput-behov: dokumentér i Noter og spring til næste pakke
- Hvis kompleks merge-conflict: spring pakken over og notér til menneskelig review

## Hændelseslog

(senest øverst)

| Tid | Hændelse |
|-----|----------|
| 16. maj 2026 | Pakke 8 afsluttet 🟢 — A20 (klikbare kort i events-/members-/messages-/my-events-/incoming-interests-page wrappet i `<a href>` med preventDefault → navigate, så tastatur-Enter/Space virker automatisk + ctrl/cmd-klik kan åbne i ny fane; `focus-within:ring-2` på Card så fokus-ring vises ved tastatur-fokus), A22 (ny `SkeletonGrid`/`FormSkeleton`/`ConversationSkeleton`-komponenter i `components/layout/loading-state.tsx` der matcher final-state-layoutet — variant "members"/"events"/"my-events"/"messages" + `aria-busy="true"`; erstattet "Indlæser…"/"Henter…"-strings i 9 sider: members/events/messages/my-events/incoming-interests/profile/membership/onboarding/verification/event-detail/couple-profile/member-detail/conversation), B23 (meningsfuld alt-tekst på `<img>` overalt — face=display_name, ambient="Stemningsbillede", private="Privat billede"), B25 (ny `PageHeader`-komponent i `components/layout/page-header.tsx` med konsistent kicker → h1 → description-hierarki + actions-slot; brugt på events/members/messages/my-events/incoming-interests/profile/membership; not-found-page fik også kicker), B27 (profile-sidens 7 knapper i Kontoadministration splittet i tre semantiske sektioner med `divide-y`: Status, Genveje, Konto — slet-konto er stadig i egen Danger-card), B29 (focus-visible:ring-2 + aria-pressed på onboarding role/face-kort-knapper), B30 (optimistic UI i conversation-page: egne beskeder appendes straks med status "sending" inden POST resolver; ved fejl markeres som "failed" med retry-knap; ved success ryddes optimistic-state og reload synkroniserer med server), B31 (conversation-page composer er nu `sticky bottom-0` med `padding-bottom: max(0.75rem, env(safe-area-inset-bottom))` så iOS Safari URL-bar ikke crasher layoutet; scroll-area er `flex-1 min-h-0 overflow-y-auto` så header+composer altid er synlige), C47 (login + signup: `aria-invalid="true"` + `aria-describedby` på email/password når error-message vises; Alert har `role="alert"` så skærmlæsere annoncerer), C48 (autoComplete="email"/"current-password" på login, "nickname"/"address-level2" på onboarding — signup havde allerede), C58 (send-knap i conversation: `h-11 w-11` (44x44 WCAG minimum) i stedet for `size="icon"` (40x40)). Native confirm/prompt → Dialog erstattet i 5 flows: member-detail (block + report med kategorier+textarea + revoke-album), event-thread (slet kommentar), admin-events (slet event), membership (cancel subscription), profile (revoke album-grant). Frontend build OK. 74/74 tests grønne (10 nye: 3× PageHeader, 5× SkeletonGrid/FormSkeleton/ConversationSkeleton, 2× ConversationPage optimistic UI sending+failed-state). Udskudt (kan tages i senere pakke): partner-confirm/waitlist-confirm/admin-pages PageHeader (lavt prioritet, ingen test), admin-pages skeleton-state (Pakke 9 Admin UX). |
| 16. maj 2026 | Pakke 7 afsluttet 🟢 — A14 (Better Auth cookie-hærdet via `advanced.defaultCookieAttributes` + `useSecureCookies`: httpOnly altid på, secure i production, sameSite=lax så OAuth-flows ikke brydes; ny `isProduction`-param i createAuthService — verificeret via `set-cookie: ...; HttpOnly; SameSite=Lax` på live login-flow), A18 (ny `POST /api/webhooks/stripe` endpoint der returnerer 501 NOT_IMPLEMENTED hvis STRIPE_WEBHOOK_SECRET ikke er sat; ved sat secret valideres signatur og event logges idempotent via ny `subscription_event.stripe_event_id`-kolonne med partiel UNIQUE-index (migration 011); ny `SubscriptionEventLog`-DI-port + `PostgresSubscriptionEventLog` med ON CONFLICT DO NOTHING; WARN-kommentarer på `startSubscription` der dokumenterer at i live-Stripe må metoden kun kaldes fra webhook), B13 (per-scope rate-limits: `login_attempt` 10/min pr. email+fingerprint, `signup_attempt` 5/time pr. fingerprint, `message_send` 30/min pr. user, `interest_signal` 20/min pr. user, `upload` 10/min pr. user; ny SCOPE_CONFIG-baseret routing i RedisRateLimiter; middleware wrapper på `/api/auth/sign-in/email` og `sign-up/email` der parser body, smugler email ind for login og bucketes på fingerprint for signup; `checkUserRateLimit` i messaging-routes + `checkUploadRateLimit` i membership-routes; 10 nye envs i .env + .env.example — verificeret via smoke-test: 15 hurtige login-forsøg gav 9× HTTP 401 efterfulgt af 6× HTTP 429), B14 (ny `validateProductionOrigins`-funktion i config der kaster fejl hvis production CORS_ORIGINS indeholder "localhost"/"127.0.0.1"/"0.0.0.0"/"*"; 6 nye tests dækker både readConfig og standalone validator), B20 (email-validering konservativ: max 254 tegn (RFC 5321), max 64 tegn local-part, separate regexes for local-part og domain, TLD-krav 2+ tegn, blokerer dobbelt-punktum, whitespace og kontrol-tegn; 5 nye tests for både accept- og afvisnings-cases), B21 (magic-bytes-detection i `detectImageMimeType`: JPEG=FFD8FF, PNG=89504E47..., WebP=RIFF...WEBP, HEIC/HEIF=ftyp+brand-set; afviser hvis claimed type ikke matcher detected; HEIC/HEIF behandles som kompatible; canonical mime sættes fra detected i stedet for klient-leveret type så vi aldrig gemmer "image/png" med JPEG-bytes; 8 nye tests; ny MIME_MISMATCH-fejlkode tilføjet til upload-routes), B22 (`hashToken` skifter fra `createHash("sha256")` til `createHmac("sha256", betterAuthSecret)` så DB-leak ikke giver brute-force-evne på tokens; fallback til SHA-256 hvis secret tomt for backwards compat i tests; tokenHashSecret tilføjet til AppDeps og threades fra index.ts). Backend boot OK efter migration 011 anvendt og rebuild. 55/55 backend-tests + 64/64 frontend-tests grønne. Type-check rent for rørte filer (kun præ-eksisterende Redis-type-conflict tilbage). |
| 16. maj 2026 | Pakke 3 afsluttet 🟢 — A2 (match-relation aktiveret: `PostgresMembershipRepository.getPublicProfile` returnerer nu `relation="match"` ved gensidig interesse, via ny `MatchChecker`-port injiceret efter construction for at undgå cirkulær DI mellem membership og messaging; ny `MembershipRepository.isMatched(viewerId, targetId)` eksponerer match-tjek til routes-laget; `GET /api/members/photo/:id` afløser hardcoded 403 for `visibility="match"` med faktisk mutual-interest-tjek, og gates derudover face-photos med `visibility="verified"` når ejeren har `face_visibility="after_interest"` så direkte foto-URL ikke kan bypasse), B1 (ny `/interests/incoming`-side med liste over indkomne signaler, beriget med afsenderprofil + ambient/face-cover (respekterer face_visibility), "Vis interesse tilbage"-CTA der åbner samtale ved mutual; banner på `/profile` med antal + link "Se hvem"), B2 (privat-album-grant UI: ny "Privat album"-knap på `/members/:id` ved `relation="match"` eller `"private_grant"` der kalder `api.grantPrivateAlbum`/`revokePrivateAlbum`; ny `api.listAlbumGrants()`-helper + sektion "Mit private album" på `/profile` med recipient_user_id, granted_at, view_count, last_viewed_at og revoke-knap pr. modtager), B48 (face-photos vises korrekt efter match via `filterPhotosForViewer`; private fotos blokeres bag overlay "Du har adgang til [navn] private album. Klik for at se ({N} billeder). Ejeren ser at du har set dem." så `view_count` ikke inflateres af auto-load — `albumRevealed`-state nulstilles ved navigation til ny profil). Backend smoke-test bekræftet: Alice signaler Bob → 1-sidet, relation=verified · Bob signaler Alice → mutual, relation=match for begge · match-visibility face-photo synligt for Alice (200), blokeret for Carla (403 MATCH_REQUIRED) · face_visibility=after_interest+verified-photo blokeret for ikke-matched (403 FACE_HIDDEN), synligt for matched · grant → relation=private_grant, view_count inkrementeres ved fetch. Backend type-check rent for rørte filer (præ-eksisterende Redis-fejl uberørt). Frontend build OK, 64/64 tests grønne (3 nye for IncomingInterestsPage: empty, list, signal-back→mutual). |
| 16. maj 2026 | Pakke 6 afsluttet 🟢 — A5 (ny `/code-of-conduct`-side med tre niveau-faner via shadcn Tabs: sanseligt-socialt, sensuelt, eksplicit; hver fane har hvad-aftenen-indebærer, hvad-er-ikke-OK, samtykke-principper, dresscode, exit-strategi og konsekvenser; URL-hash-deep-link til specifikt niveau (#sensual-social, #sensual, #explicit); link tilføjet i header public-nav (desktop + mobile)), B46 (ny `/terms`-side med 9 nøgleafsnit: hvem driver Glød, hvad Glød er, medlemskab+pris+opsigelse, 14-dages fortrydelsesret, event-tilmelding+afmelding, hvad-ikke-tilladt, moderation+konsekvenser, refundering, 30-dages varsel ved vilkårsændringer, tvister+værneting), global footer (`<footer data-testid="site-footer">`) med fire links: Privatliv / Vilkår / Code of conduct / Kontakt — vises overalt undtagen i conversation-page (`/messages/:id`), signup-page checkbox-tekst opdateret til tre klikbare links (privacy + terms + code of conduct), landing-page waitlist checkbox-tekst gjort klikbar (handelsbetingelser + persondatapolitik), event-detail-page tilføjet "Læs code of conduct for {LEVEL}-events →" under tilmeldings-knap med deep-link til relevant fane. Frontend build OK, 59/59 tests grønne (4 nye: 2× CodeOfConductPage tab-rendering+tab-skift, 2× TermsPage rendering+nøglesektioner). |
| 16. maj 2026 | Pakke 5 afsluttet 🟢 — A9 (Slet konto-sektion på `/profile` med to-trins Dialog: soft- vs. hard-delete, email-bekræftelse, GDPR-rettigheds-info + link til privacy), A10 (hardDelete er nu GDPR-konform: anonymiserer user-rækken til `[Slettet bruger]` med dummy-email, sletter profile_photo + verification_submission og deres fysiske filer, opløser couple_profile, annullerer subscriptions/interest_signal/invitations, invaliderer sessions; transaktionel — beskeder bevares så samtale-partnere stadig kan se historikken via ny `getProfileIncludingDeleted`-metode), A17 (globalt ghost-protection-middleware på `/api/*` checker `deleted_at` ved enhver auth'd request; soft-deleted brugere får 401 ACCOUNT_DELETED selv med gyldig session-cookie; `/api/auth/*` springes over så logout virker), A19 (navngiven dataansvarlig "Mikkel Freltoft Krogsholm" i privacy-page + JSON-LD `contactPoint`/`founder`; nye envs `DATA_CONTROLLER_NAME` + `DATA_CONTROLLER_EMAIL`; email-signaturer bruger nu dataansvarlig-info; alle `support@findgloed.dk` skiftet til `mikkel@findgloed.dk`), C9 (par-koblinger opløses automatisk i både soft- og hardDelete; pending invitations cancelleres). Backend smoke-test bekræftet: oprettelse → mutual interest → besked → hardDelete → "[Slettet bruger]" vises i samtale-partnerens chat, deleted user får 401, photos/sessions/IDs er væk fra DB. Frontend build + 55/55 tests grønne (ny test for delete-dialog). |
| 16. maj 2026 | Pakke 4 afsluttet 🟢 — A15 (MitID-over-claim fjernet i vision FAQ + index.html meta), A16 ("trygt"/"tryghed"/"trygge rammer" fjernet fra landing/vision/signup/membership/partner-modal/design-page/design-system; nu kun 0 referencer i kodebasen — beslutning 9 er låst), B12 (ny "To roller, ligeværdige"-sektion på vision-page + tagline på landing om "den der inviterer / bestemmer tempoet"), C33-C43 (seed-event copy renset for tantra/sex-positive/redskaber/terapeut, event-thread "kødmarked"-bisætning fjernet, onboarding "adressere respektfuldt" → ligeværdige roller, engelsk "invites/early access" oversat, landing-tankestreg). Frontend build + 45/45 tests grønne. |
| 16. maj 2026 | Pakke 1 afsluttet 🟢 — A6 (SQL ambiguous fix), A7 (ghost-medlemmer filter), A8 (admin-verifications + admin-reports UI), A21 (onboarding-knap-tekst), A23 (rejectVerification nedgraderer altid). Smoke-test bekræftet: `/api/admin/verifications` returnerer 200, ghost-bruger filtreres ud af `/api/members`. Build + 45/45 tests grønne. |
| (init) | Loop-progress oprettet. Starter Pakke 1. |
